[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx turbo run build --filter=@cryptovix/web"

[deploy]
startCommand = "npx next start -p $PORT"
workingDirectory = "apps/web"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
